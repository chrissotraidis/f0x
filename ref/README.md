# Local reference area

`ref/` is intentionally ignored except for this safety note.

It may contain local clones of upstream projects, a user-owned F-Zero X ROM,
and development-only generated assets. None of those inputs may be staged,
committed, packaged, or redistributed from F0X.

The pinned runtime baseline is recorded in [`../docs/DEPENDENCIES.md`](../docs/DEPENDENCIES.md).
